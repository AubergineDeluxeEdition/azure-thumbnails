{
  "name": "azure-thumbnails",
  "version": "1.0.0",
  "description": "Azure function to generate image thumbnails using blob triggers.",
  "main": "src/functions/thumbnails.js",
  "scripts": {
    "start": "func start",
    "build": "npm install",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.6.0",
    "jimp": "^1.6.0"
  },
  "devDependencies": {
    "azure-functions-core-tools": "^4.x"
  }
}
